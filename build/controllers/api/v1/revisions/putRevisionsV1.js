// Generated by CoffeeScript 1.6.3
(function() {
  var ORM, apiAuth, apiVerifyObjectProperties, async, config, sequelize, updateHelper, uuid, _;

  _ = require('underscore');

  async = require('async');

  uuid = require('node-uuid');

  config = require(GLOBAL.appRoot + 'config/config');

  apiVerifyObjectProperties = require(GLOBAL.appRoot + 'components/apiVerifyObjectProperties');

  apiAuth = require(GLOBAL.appRoot + 'components/apiAuth');

  ORM = require(GLOBAL.appRoot + 'components/oRM');

  updateHelper = require(GLOBAL.appRoot + 'components/updateHelper');

  sequelize = ORM.setup();

  module.exports = function(app) {
    var employee, revision, template;
    template = ORM.model('template');
    revision = ORM.model('revision');
    employee = ORM.model('employee');
    return app.put(config.apiSubDir + '/v1/revisions', function(req, res) {
      return async.series([
        function(callback) {
          return apiAuth(req, res, callback);
        }, function(callback) {
          var clientUid, employeeUid, userType;
          userType = req.session.user.type;
          clientUid = req.session.user.clientUid;
          employeeUid = req.session.user.uid;
          switch (userType) {
            case 'superAdmin':
              return apiVerifyObjectProperties(this, template, req.body, req, res, false, {
                requiredProperties: {
                  'uid': function(val, objectKey, object, callback) {
                    if (_.isUndefined(val)) {
                      return callback(null, {
                        success: false,
                        message: {
                          uid: 'required'
                        }
                      });
                    } else {
                      return revision.find({
                        where: {
                          uid: val
                        }
                      }).success(function(resultRevision) {
                        var mapObj;
                        if (resultRevision) {
                          mapObj = {};
                          mapObj[val] = resultRevision;
                          return callback(null, {
                            success: true,
                            uidMapping: mapObj
                          });
                        } else {
                          return callback(null, {
                            success: false,
                            message: {
                              uid: 'unknown'
                            }
                          });
                        }
                      });
                    }
                  },
                  'changeSummary': function(val, objectKey, object, callback) {
                    return callback(null, {
                      success: true
                    });
                  },
                  'scope': function(val, objectKey, object, callback) {
                    return callback(null, {
                      success: true
                    });
                  },
                  'finalized': function(val, objectKey, object, callback) {
                    var transVal;
                    if (_.isUndefined(val)) {
                      callback(null, {
                        success: true
                      });
                      return;
                    }
                    if (val !== 'true' && val !== 'false' && (val === !true) && (val === !false)) {
                      callback(null, {
                        success: false,
                        message: {
                          finalized: 'invalid'
                        }
                      });
                      return;
                    }
                    transVal = val === 'true' || (val === true) ? true : false;
                    return callback(null, {
                      success: true,
                      transform: [objectKey, 'finalized', transVal]
                    });
                  }
                }
              }, function(objects) {
                return updateHelper(revision, objects, req, res, app);
              });
            case 'clientSuperAdmin':
            case 'clientAdmin':
              return apiVerifyObjectProperties(this, template, req.body, req, res, false, {
                requiredProperties: {
                  'uid': function(val, objectKey, object, callback) {
                    if (_.isUndefined(val)) {
                      return callback(null, {
                        success: false,
                        message: {
                          uid: 'required'
                        }
                      });
                    } else {
                      return revision.find({
                        where: {
                          clientUid: clientUid,
                          uid: val
                        }
                      }).success(function(resultRevision) {
                        var mapObj;
                        if (resultRevision) {
                          mapObj = {};
                          mapObj[val] = resultRevision;
                          return callback(null, {
                            success: true,
                            uidMapping: mapObj
                          });
                        } else {
                          return callback(null, {
                            success: false,
                            message: {
                              uid: 'unknown'
                            }
                          });
                        }
                      });
                    }
                  },
                  'changeSummary': function(val, objectKey, object, callback) {
                    return callback(null, {
                      success: true
                    });
                  },
                  'scope': function(val, objectKey, object, callback) {
                    return callback(null, {
                      success: true
                    });
                  },
                  'finalized': function(val, objectKey, object, callback) {
                    var transVal;
                    if (_.isUndefined(val)) {
                      callback(null, {
                        success: true
                      });
                      return;
                    }
                    if (val !== 'true' && val !== 'false' && (val === !true) && (val === !false)) {
                      callback(null, {
                        success: false,
                        message: {
                          finalized: 'invalid'
                        }
                      });
                      return;
                    }
                    transVal = val === 'true' || (val === true) ? true : false;
                    return callback(null, {
                      success: true,
                      transform: [objectKey, 'finalized', transVal]
                    });
                  }
                }
              }, function(objects) {
                return updateHelper(revision, objects, req, res, app);
              });
            case 'clientDelegate':
            case 'clientAuditor':
              return res.jsonAPIRespond(config.errorResponse(401));
          }
        }
      ]);
    });
  };

}).call(this);
