// Generated by CoffeeScript 1.6.3
(function() {
  var ORM, apiAuth, apiVerifyObjectProperties, async, config, sequelize, updateHelper, uuid, _;

  _ = require('underscore');

  async = require('async');

  uuid = require('node-uuid');

  config = require(GLOBAL.appRoot + 'config/config');

  apiVerifyObjectProperties = require(GLOBAL.appRoot + 'components/apiVerifyObjectProperties');

  apiAuth = require(GLOBAL.appRoot + 'components/apiAuth');

  ORM = require(GLOBAL.appRoot + 'components/oRM');

  updateHelper = require(GLOBAL.appRoot + 'components/updateHelper');

  sequelize = ORM.setup();

  module.exports = function(app) {
    var client, dictionary, employee;
    client = ORM.model('client');
    dictionary = ORM.model('dictionary');
    employee = ORM.model('employee');
    return app.put(config.apiSubDir + '/v1/dictionaries', function(req, res) {
      return async.series([
        function(callback) {
          return apiAuth(req, res, callback);
        }, function(callback) {
          var clientUid, userType;
          userType = req.session.user.type;
          clientUid = req.session.user.clientUid;
          switch (userType) {
            case 'superAdmin':
              return apiVerifyObjectProperties(this, dictionary, req.body, req, res, false, {
                requiredProperties: {
                  'uid': function(val, objectKey, object, callback) {
                    if (_.isUndefined(val)) {
                      return callback(null, {
                        success: false,
                        message: {
                          uid: 'required'
                        }
                      });
                    } else {
                      return dictionary.find({
                        where: {
                          uid: val
                        }
                      }).success(function(resultDictionary) {
                        var mapObj;
                        if (resultDictionary) {
                          mapObj = {};
                          mapObj[val] = resultDictionary;
                          return callback(null, {
                            success: true,
                            uidMapping: mapObj
                          });
                        } else {
                          return callback(null, {
                            success: false,
                            message: {
                              uid: 'unknown'
                            }
                          });
                        }
                      });
                    }
                  },
                  'name': function(val, objectKey, object, callback) {
                    return callback(null, {
                      success: true
                    });
                  },
                  'clientUid': function(val, objectKey, object, callback) {
                    if (_.isUndefined(val)) {
                      return callback(null, {
                        success: true
                      });
                    } else {
                      return callback(null, {
                        success: false,
                        message: {
                          clientUid: 'unknown'
                        }
                      });
                    }
                  },
                  'deletedAt': function(val, objectKey, object, callback) {
                    return callback(null, {
                      success: true
                    });
                  }
                }
              }, function(objects) {
                return updateHelper(dictionary, objects, req, res, app);
              });
            case 'clientSuperAdmin':
            case 'clientAdmin':
              return apiVerifyObjectProperties(this, dictionary, req.body, req, res, false, {
                requiredProperties: {
                  'uid': function(val, objectKey, object, callback) {
                    if (_.isUndefined(val)) {
                      return callback(null, {
                        success: false,
                        message: {
                          uid: 'required'
                        }
                      });
                    } else {
                      return dictionary.find({
                        where: {
                          clientUid: clientUid,
                          uid: val
                        }
                      }).success(function(resultDictionary) {
                        var mapObj;
                        if (resultDictionary) {
                          mapObj = {};
                          mapObj[val] = resultDictionary;
                          return callback(null, {
                            success: true,
                            uidMapping: mapObj
                          });
                        } else {
                          return callback(null, {
                            success: false,
                            message: {
                              uid: 'unknown'
                            }
                          });
                        }
                      });
                    }
                  },
                  'name': function(val, objectKey, object, callback) {
                    return callback(null, {
                      success: true
                    });
                  },
                  'clientUid': function(val, objectKey, object, callback) {
                    if (_.isUndefined(val)) {
                      return callback(null, {
                        success: true
                      });
                    } else {
                      return callback(null, {
                        success: false,
                        message: {
                          clientUid: 'unknown'
                        }
                      });
                    }
                  },
                  'deletedAt': function(val, objectKey, object, callback) {
                    return callback(null, {
                      success: true
                    });
                  }
                }
              }, function(objects) {
                return updateHelper(dictionary, objects, req, res, app);
              });
            case 'clientDelegate':
            case 'clientAuditor':
              return res.jsonAPIRespond(config.errorResponse(401));
          }
        }
      ]);
    });
  };

}).call(this);
