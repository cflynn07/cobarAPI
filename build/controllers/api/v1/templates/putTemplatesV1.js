// Generated by CoffeeScript 1.6.3
(function() {
  var ORM, apiAuth, apiVerifyObjectProperties, async, config, sequelize, updateHelper, uuid, _;

  _ = require('underscore');

  async = require('async');

  uuid = require('node-uuid');

  config = require(GLOBAL.appRoot + 'config/config');

  apiVerifyObjectProperties = require(GLOBAL.appRoot + 'components/apiVerifyObjectProperties');

  apiAuth = require(GLOBAL.appRoot + 'components/apiAuth');

  ORM = require(GLOBAL.appRoot + 'components/oRM');

  updateHelper = require(GLOBAL.appRoot + 'components/updateHelper');

  sequelize = ORM.setup();

  module.exports = function(app) {
    var employee, template;
    template = ORM.model('template');
    employee = ORM.model('employee');
    return app.put(config.apiSubDir + '/v1/templates', function(req, res) {
      return async.series([
        function(callback) {
          return apiAuth(req, res, callback);
        }, function(callback) {
          var clientUid, employeeUid, userType;
          userType = req.session.user.type;
          clientUid = req.session.user.clientUid;
          employeeUid = req.session.user.uid;
          switch (userType) {
            case 'superAdmin':
              return apiVerifyObjectProperties(this, template, req.body, req, res, false, {
                requiredProperties: {
                  'uid': function(val, objectKey, object, callback) {
                    if (_.isUndefined(val)) {
                      return callback(null, {
                        success: false,
                        message: {
                          uid: 'required'
                        }
                      });
                    } else {
                      return template.find({
                        where: {
                          uid: val
                        }
                      }).success(function(resultTemplate) {
                        var mapObj;
                        if (resultTemplate) {
                          mapObj = {};
                          mapObj[val] = resultTemplate;
                          return callback(null, {
                            success: true,
                            uidMapping: mapObj
                          });
                        } else {
                          return callback(null, {
                            success: false,
                            message: {
                              uid: 'unknown'
                            }
                          });
                        }
                      });
                    }
                  },
                  'name': function(val, objectKey, object, callback) {
                    return callback(null, {
                      success: true
                    });
                  },
                  'type': function(val, objectKey, object, callback) {
                    return callback(null, {
                      success: true
                    });
                  },
                  'deletedAt': function(val, objectKey, object, callback) {
                    if (_.isUndefined(val)) {
                      callback(null, {
                        success: true
                      });
                      return;
                    }
                    if ((val !== 'null') && (val === !null)) {
                      callback(null, {
                        success: false,
                        message: {
                          deletedAt: 'invalid'
                        }
                      });
                      return;
                    }
                    return callback(null, {
                      success: true,
                      transform: [objectKey, 'deletedAt', null]
                    });
                  },
                  'employeeUid': function(val, objectKey, object, callback) {
                    var callbacks, uid;
                    uid = object['uid'];
                    if (_.isUndefined(uid)) {
                      callback(null, {
                        success: false
                      });
                      return;
                    }
                    if (_.isUndefined(val)) {
                      val = employeeUid;
                    }
                    callbacks = [];
                    (function(val) {
                      callbacks.push(function(callback) {
                        return employee.find({
                          where: {
                            uid: val
                          }
                        }).success(function(resultEmployee) {
                          return callback(null, resultEmployee);
                        });
                      });
                      return callbacks.push(function(callback) {
                        return template.find({
                          where: {
                            uid: uid
                          }
                        }).success(function(resultTemplate) {
                          return callback(null, resultTemplate);
                        });
                      });
                    })(val);
                    return async.parallel(callbacks, function(err, results) {
                      var mapObj, resultEmployee, resultTemplate;
                      resultEmployee = results[0];
                      resultTemplate = results[1];
                      if (!resultEmployee) {
                        callback(null, {
                          success: false,
                          message: {
                            'employeeUid': 'unknown'
                          }
                        });
                        return;
                      }
                      if (!resultTemplate) {
                        callback(null, {
                          success: false
                        });
                        return;
                      }
                      if (resultEmployee.clientUid !== resultTemplate.clientUid) {
                        callback(null, {
                          success: false,
                          message: {
                            'employeeUid': 'unknown'
                          }
                        });
                        return;
                      }
                      mapObj = {};
                      mapObj[resultEmployee.uid] = resultEmployee;
                      return callback(null, {
                        success: true,
                        uidMapping: mapObj,
                        transform: [objectKey, 'employeeUid', resultEmployee.uid]
                      });
                    });
                  }
                }
              }, function(objects) {
                return updateHelper(template, objects, req, res, app);
              });
            case 'clientSuperAdmin':
            case 'clientAdmin':
              return apiVerifyObjectProperties(this, template, req.body, req, res, false, {
                requiredProperties: {
                  'uid': function(val, objectKey, object, callback) {
                    if (_.isUndefined(val)) {
                      return callback(null, {
                        success: false,
                        message: {
                          uid: 'required'
                        }
                      });
                    } else {
                      return template.find({
                        where: {
                          uid: val,
                          clientUid: clientUid
                        }
                      }).success(function(resultTemplate) {
                        var mapObj;
                        if (resultTemplate) {
                          mapObj = {};
                          mapObj[val] = resultTemplate;
                          return callback(null, {
                            success: true,
                            uidMapping: mapObj
                          });
                        } else {
                          return callback(null, {
                            success: false,
                            message: {
                              uid: 'unknown'
                            }
                          });
                        }
                      });
                    }
                  },
                  'name': function(val, objectKey, object, callback) {
                    return callback(null, {
                      success: true
                    });
                  },
                  'type': function(val, objectKey, object, callback) {
                    return callback(null, {
                      success: true
                    });
                  },
                  'deletedAt': function(val, objectKey, object, callback) {
                    if (_.isUndefined(val)) {
                      callback(null, {
                        success: true
                      });
                      return;
                    }
                    if ((val !== 'null') && (val === !null)) {
                      callback(null, {
                        success: false,
                        message: {
                          deletedAt: 'invalid'
                        }
                      });
                      return;
                    }
                    return callback(null, {
                      success: true
                    });
                  },
                  'employeeUid': function(val, objectKey, object, callback) {
                    var callbacks, uid;
                    uid = object['uid'];
                    if (_.isUndefined(uid)) {
                      callback(null, {
                        success: false
                      });
                      return;
                    }
                    if (_.isUndefined(val)) {
                      val = employeeUid;
                    }
                    callbacks = [];
                    (function(val) {
                      callbacks.push(function(callback) {
                        return employee.find({
                          where: {
                            uid: val,
                            clientUid: clientUid
                          }
                        }).success(function(resultEmployee) {
                          return callback(null, resultEmployee);
                        });
                      });
                      return callbacks.push(function(callback) {
                        return template.find({
                          where: {
                            uid: uid,
                            clientUid: clientUid
                          }
                        }).success(function(resultTemplate) {
                          return callback(null, resultTemplate);
                        });
                      });
                    })(val);
                    return async.parallel(callbacks, function(err, results) {
                      var mapObj, resultEmployee, resultTemplate;
                      resultEmployee = results[0];
                      resultTemplate = results[1];
                      if (!resultEmployee) {
                        callback(null, {
                          success: false,
                          message: {
                            employeeUid: 'unknown'
                          }
                        });
                        return;
                      }
                      if (!resultTemplate) {
                        callback(null, {
                          success: false
                        });
                        return;
                      }
                      if (resultEmployee.clientUid !== resultTemplate.clientUid) {
                        callback(null, {
                          success: false,
                          message: {
                            employeeUid: 'unknown'
                          }
                        });
                        return;
                      }
                      mapObj = {};
                      mapObj[resultEmployee.uid] = resultEmployee;
                      return callback(null, {
                        success: true,
                        uidMapping: mapObj,
                        transform: [objectKey, 'employeeUid', resultEmployee.uid]
                      });
                    });
                  }
                }
              }, function(objects) {
                return updateHelper(template, objects, req, res, app);
              });
            case 'clientDelegate':
            case 'clientAuditor':
              return res.jsonAPIRespond(config.errorResponse(401));
          }
        }
      ]);
    });
  };

}).call(this);
