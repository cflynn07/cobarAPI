// Generated by CoffeeScript 1.6.3
(function() {
  var ORM, apiAuth, apiVerifyObjectProperties, async, config, sequelize, updateHelper, uuid, _;

  _ = require('underscore');

  async = require('async');

  uuid = require('node-uuid');

  config = require(GLOBAL.appRoot + 'config/config');

  apiVerifyObjectProperties = require(GLOBAL.appRoot + 'components/apiVerifyObjectProperties');

  apiAuth = require(GLOBAL.appRoot + 'components/apiAuth');

  updateHelper = require(GLOBAL.appRoot + 'components/updateHelper');

  ORM = require(GLOBAL.appRoot + 'components/oRM');

  sequelize = ORM.setup();

  module.exports = function(app) {
    var client, dictionaryItem;
    client = ORM.model('client');
    dictionaryItem = ORM.model('dictionaryItem');
    return app.put(config.apiSubDir + '/v1/dictionaryItems', function(req, res) {
      return async.series([
        function(callback) {
          return apiAuth(req, res, callback);
        }, function(callback) {
          var clientUid, employeeUid, userType;
          userType = req.session.user.type;
          clientUid = req.session.user.clientUid;
          employeeUid = req.session.user.uid;
          switch (userType) {
            case 'superAdmin':
              return apiVerifyObjectProperties(this, dictionaryItem, req.body, req, res, false, {
                requiredProperties: {
                  'uid': function(val, objectKey, object, callback) {
                    if (_.isUndefined(val)) {
                      return callback(null, {
                        success: false,
                        message: {
                          uid: 'required'
                        }
                      });
                    } else {
                      return dictionaryItem.find({
                        where: {
                          uid: val
                        }
                      }).success(function(resultResource) {
                        var mapObj;
                        if (resultResource) {
                          mapObj = {};
                          mapObj[val] = resultResource;
                          return callback(null, {
                            success: true,
                            uidMapping: mapObj
                          });
                        } else {
                          return callback(null, {
                            success: false,
                            message: {
                              uid: 'unknown'
                            }
                          });
                        }
                      });
                    }
                  },
                  'name': function(val, objectKey, object, callback) {
                    return callback(null, {
                      success: true
                    });
                  },
                  'clientUid': function(val, objectKey, object, callback) {
                    if (_.isUndefined(val)) {
                      return callback(null, {
                        success: true
                      });
                    } else {
                      return callback(null, {
                        success: false,
                        message: {
                          clientUid: 'unknown'
                        }
                      });
                    }
                  }
                }
              }, function(objects) {
                return updateHelper(dictionaryItem, objects, req, res, app);
              });
            case 'clientSuperAdmin':
            case 'clientAdmin':
              return apiVerifyObjectProperties(this, dictionaryItem, req.body, req, res, false, {
                requiredProperties: {
                  'uid': function(val, objectKey, object, callback) {
                    if (_.isUndefined(val)) {
                      return callback(null, {
                        success: false,
                        message: {
                          uid: 'required'
                        }
                      });
                    } else {
                      return dictionaryItem.find({
                        where: {
                          clientUid: clientUid,
                          uid: val
                        }
                      }).success(function(resultResource) {
                        var mapObj;
                        if (resultResource) {
                          mapObj = {};
                          mapObj[val] = resultResource;
                          return callback(null, {
                            success: true,
                            uidMapping: mapObj
                          });
                        } else {
                          return callback(null, {
                            success: false,
                            message: {
                              uid: 'unknown'
                            }
                          });
                        }
                      });
                    }
                  },
                  'name': function(val, objectKey, object, callback) {
                    return callback(null, {
                      success: true
                    });
                  },
                  'clientUid': function(val, objectKey, object, callback) {
                    if (_.isUndefined(val)) {
                      return callback(null, {
                        success: true
                      });
                    } else {
                      return callback(null, {
                        success: false,
                        message: {
                          clientUid: 'unknown'
                        }
                      });
                    }
                  }
                }
              }, function(objects) {
                return updateHelper(dictionaryItem, objects, req, res, app);
              });
            case 'clientDelegate':
            case 'clientAuditor':
              return res.jsonAPIRespond(config.errorResponse(401));
          }
        }
      ]);
    });
  };

}).call(this);
